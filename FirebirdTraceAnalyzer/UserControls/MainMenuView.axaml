<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:FirebirdTraceAnalyzer.ViewModels"
             xmlns:reports="clr-namespace:FirebirdTraceAnalyzer.Models.Reports"
             xmlns:loc="clr-namespace:FirebirdTraceAnalyzer.Localization"
             mc:Ignorable="d" d:DesignWidth="1100"
             x:Class="FirebirdTraceAnalyzer.UserControls.MainMenuView"
             x:DataType="vm:MainWindowViewModel">

    <!-- In-window меню (используется на платформах без нативного меню; здесь же — динамические
         списки отчётов). DataContext наследуется от хост-окна (MainWindowViewModel). -->
    <Menu>

        <MenuItem Header="{loc:Tr Menu.Open}">
            <MenuItem
                Header="{loc:Tr Menu.Open.Local}"
                InputGesture="Ctrl+O"
                HotKey="Ctrl+O"
                Command="{Binding OpenLocalFileCommand}" />

            <MenuItem
                Header="{loc:Tr Menu.Open.Remote}"
                InputGesture="Ctrl+Shift+O"
                HotKey="Ctrl+Shift+O"
                Command="{Binding OpenRemoteFileCommand}" />

            <MenuItem
                Header="{loc:Tr Menu.Open.Cancel}"
                InputGesture="Ctrl+Escape"
                HotKey="Ctrl+Escape"
                Command="{Binding CancelLoadingCommand}" />

        </MenuItem>

        <MenuItem Header="{loc:Tr Menu.Edit}">

            <MenuItem Header="{loc:Tr Menu.Edit.Reparse}">
                <MenuItem
                    Header="{loc:Tr Menu.Common.AllFiles}"
                    InputGesture="Ctrl+R"
                    HotKey="Ctrl+R"
                    Command="{Binding ReparseAllFilesCommand}" />

                <MenuItem
                    Header="{loc:Tr Menu.Common.SelectedFiles}"
                    InputGesture="Ctrl+Shift+R"
                    HotKey="Ctrl+Shift+R"
                    Command="{Binding ReparseSelectedFilesCommand}" />
            </MenuItem>

            <MenuItem Header="{loc:Tr Menu.Edit.Close}">
                <MenuItem
                    Header="{loc:Tr Menu.Common.AllFiles}"
                    InputGesture="Ctrl+Shift+W"
                    HotKey="Ctrl+Shift+W"
                    Command="{Binding CloseAllFilesCommand}" />

                <MenuItem
                    Header="{loc:Tr Menu.Common.SelectedFiles}"
                    InputGesture="Ctrl+W"
                    HotKey="Ctrl+W"
                    Command="{Binding CloseSelectedFilesCommand}" />
            </MenuItem>

        </MenuItem>

        <MenuItem Header="{loc:Tr Menu.Reports}">

            <!-- Динамические встроенные отчеты (Quick Reports) -->
            <MenuItem Header="{loc:Tr Menu.Reports.Quick}" ItemsSource="{Binding BuiltInReports}">
                <MenuItem.Styles>
                    <!-- Этот стиль применяется ко всем сгенерированным дочерним элементам -->
                    <Style Selector="MenuItem" x:DataType="reports:ReportTemplate">
                        <Setter Property="Header" Value="{Binding Name}" />
                        <!-- Биндимся к команде в главной ViewModel, передавая Id шаблона -->
                        <Setter Property="Command" Value="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).GenerateQuickReportCommand}" />
                        <Setter Property="CommandParameter" Value="{Binding Id}" />
                        <!-- Бонус: показываем описание при наведении -->
                        <Setter Property="ToolTip.Tip" Value="{Binding Description}" />
                    </Style>
                </MenuItem.Styles>
            </MenuItem>

            <!-- Динамические пользовательские отчеты (Custom Reports) -->
            <MenuItem Header="{loc:Tr Menu.Reports.Custom}"
                      ItemsSource="{Binding CustomReports}"
                      IsVisible="{Binding CustomReports.Count, Converter={x:Static ObjectConverters.IsNotNull}}">
                <!-- Показываем это меню только если есть хотя бы один кастомный отчет -->
                <MenuItem.Styles>
                    <Style Selector="MenuItem" x:DataType="reports:ReportTemplate">
                        <Setter Property="Header" Value="{Binding Name}" />
                        <Setter Property="Command" Value="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).GenerateQuickReportCommand}" />
                        <Setter Property="CommandParameter" Value="{Binding Id}" />
                        <Setter Property="ToolTip.Tip" Value="{Binding Description}" />
                    </Style>
                </MenuItem.Styles>
            </MenuItem>

            <Separator />

            <!-- Manage Templates (single entry → in-window overlay) -->
            <MenuItem
                Header="{loc:Tr Menu.Reports.Manage}"
                Command="{Binding OpenManageTemplatesCommand}" />

            <Separator />

            <!-- History -->
            <MenuItem
                Header="{loc:Tr Menu.Reports.Recent}"
                Command="{Binding OpenRecentReportsCommand}" />

        </MenuItem>

        <MenuItem Header="{loc:Tr Menu.Window}">
            <MenuItem
                Header="{loc:Tr Menu.Window.Files}"
                InputGesture="Ctrl+D1"
                HotKey="Ctrl+D1"
                Command="{Binding SwitchVisibleTraceFilesSectionCommand}"
                ToggleType="CheckBox"
                IsChecked="{Binding IsTraceFilesSectionVisible}"
                StaysOpenOnClick="True" />

            <MenuItem
                Header="{loc:Tr Menu.Window.Search}"
                InputGesture="Ctrl+D2"
                HotKey="Ctrl+D2"
                Command="{Binding SwitchVisibleSearchSectionCommand}"
                ToggleType="CheckBox"
                IsChecked="{Binding IsSearchSectionVisible}"
                StaysOpenOnClick="True" />

            <MenuItem
                Header="{loc:Tr Menu.Window.Events}"
                InputGesture="Ctrl+D3"
                HotKey="Ctrl+D3"
                IsChecked="{Binding IsEventsSectionVisible}"
                Command="{Binding SwitchEventsSectionVisibleCommand}"
                ToggleType="CheckBox"
                StaysOpenOnClick="True" />

            <MenuItem
                Header="{loc:Tr Menu.Window.Statistics}"
                InputGesture="Ctrl+D4"
                HotKey="Ctrl+D4"
                IsChecked="{Binding IsStatisticsSectionVisible}"
                Command="{Binding SwitchStatisticsSectionVisibleCommand}"
                ToggleType="CheckBox"
                StaysOpenOnClick="True" />

            <MenuItem
                Header="{loc:Tr Menu.Window.Logs}"
                InputGesture="Ctrl+D5"
                HotKey="Ctrl+D5"
                IsChecked="{Binding IsLogsSectionVisible}"
                Command="{Binding SwitchLogsSectionVisibleCommand}"
                ToggleType="CheckBox"
                StaysOpenOnClick="True" />

            <Separator />
            <MenuItem
                Header="{loc:Tr Menu.Window.Reset}"
                InputGesture="Ctrl+D0"
                HotKey="Ctrl+D0"
                Command="{Binding GoToFactorySettingsSectionCommand}" />

        </MenuItem>

        <MenuItem Header="{loc:Tr Menu.Storage}">
            <MenuItem Header="{loc:Tr Menu.StorageManagement}" Command="{Binding OpenStoreManagementCommand}"/>
            <MenuItem Header="{loc:Tr Menu.StorageAnalysis}" Command="{Binding OpenStorageAnalysisCommand}" />
        </MenuItem>

        <MenuItem Header="{loc:Tr Menu.ParserStats}"
                  Command="{Binding OpenParserStatisticsCommand}"
                  IsVisible="{Binding IsStatisticsMode}" />

        <MenuItem Header="{loc:Tr Menu.Plugins}" Command="{Binding OpenPluginsCommand}" />

        <MenuItem Header="{loc:Tr Menu.Settings}"
                  InputGesture="Ctrl+OemComma"
                  HotKey="Ctrl+OemComma"
                  Command="{Binding OpenSettingsCommand}" />
    </Menu>
</UserControl>
