<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:FirebirdTraceAnalyzer.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:FirebirdTraceAnalyzer.Localization"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
             x:Class="FirebirdTraceAnalyzer.Views.RemoteFileSelectionView"
             x:DataType="vm:RemoteFileSelectionViewModel"
             Width="860"
             MaxHeight="680">

    <Design.DataContext>
        <vm:RemoteFileSelectionViewModel />
    </Design.DataContext>

    <Grid RowDefinitions="Auto,Auto,*,Auto" Background="{DynamicResource AppBackgroundBrush}">

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceBrush}"
                Padding="20"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">

            <StackPanel Spacing="4">
                <TextBlock Text="{loc:Tr Remote.Files.Title}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />

                <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}">
                    <Run Text="{loc:Tr Remote.Files.ServerLabel}" />
                    <Run Text="{Binding ServerInfo}" FontWeight="Medium" />
                </TextBlock>

                <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}">
                    <Run Text="{loc:Tr Remote.Files.DirectoryLabel}" />
                    <Run Text="{Binding RemoteDirectory}" FontWeight="Medium" />
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Search and Filters -->
        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                Padding="20,10"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">

            <Grid ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="8">

                <!-- Search -->
                <TextBox Grid.Column="0"
                         Text="{Binding SearchText}"
                         PlaceholderText="{loc:Tr Remote.Files.SearchPlaceholder}"
                         VerticalContentAlignment="Center">
                    <TextBox.KeyBindings>
                        <KeyBinding Gesture="Enter" Command="{Binding ApplySearchCommand}" />
                    </TextBox.KeyBindings>
                </TextBox>

                <!-- Clear Search -->
                <Button Grid.Column="1"
                        Classes="danger"
                        Command="{Binding ClearSearchCommand}"
                        IsVisible="{Binding SearchText, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                        Height="40" Width="40"
                        ToolTip.Tip="{loc:Tr Remote.Files.ClearSearch}" >
                    <Button.Content>
                        <Svg Width="40" Path="{DynamicResource ClearIcon}" Css="{DynamicResource ThemeIconFillOnlyWhite}" />
                    </Button.Content>

                </Button>

                <!-- Date Filter -->
                <Button Grid.Column="2" Height="40" Width="40" Classes="normal">
                    <Button.Content>
                        <Svg Width="40" Path="{DynamicResource FilterIcon}" Css="{DynamicResource ThemeIconStroke}" />
                    </Button.Content>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuItem Header="{loc:Tr Remote.Files.Filter.Today}" Command="{Binding FilterByDateCommand}" CommandParameter="today" />
                            <MenuItem Header="{loc:Tr Remote.Files.Filter.Yesterday}" Command="{Binding FilterByDateCommand}" CommandParameter="yesterday" />
                            <MenuItem Header="{loc:Tr Remote.Files.Filter.Last7Days}" Command="{Binding FilterByDateCommand}" CommandParameter="week" />
                            <MenuItem Header="{loc:Tr Remote.Files.Filter.Last30Days}" Command="{Binding FilterByDateCommand}" CommandParameter="month" />
                            <Separator />
                            <MenuItem Header="{loc:Tr Remote.Files.Filter.AllFiles}" Command="{Binding ClearSearchCommand}" />
                        </MenuFlyout>
                    </Button.Flyout>

                </Button>

                <!-- Refresh -->
                <Button Grid.Column="3"
                        Classes="normal"
                        Height="40" Width="40"
                        Command="{Binding RefreshCommand}"
                        ToolTip.Tip="{loc:Tr Remote.Files.RefreshList}" >
                    <Button.Content>
                        <Svg Width="40" Path="{DynamicResource ReloadIcon}" Css="{DynamicResource ThemeIconFill}" />
                    </Button.Content>
                </Button>
            </Grid>
        </Border>

        <!-- File List -->
        <Border Grid.Row="2"
                Background="{DynamicResource AppBackgroundBrush}"
                Padding="20">

            <Grid RowDefinitions="Auto,Auto,*">

                <!-- Предупреждение: включено удаление файлов с сервера после обработки -->
                <Border Grid.Row="0"
                        Background="{DynamicResource WarningSurfaceBrush}"
                        BorderBrush="{DynamicResource WarningBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12"
                        Margin="0,0,0,12"
                        IsVisible="{Binding DeleteAfterProcessing}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{loc:Tr Remote.Files.DeleteWarning.Title}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource WarningTextBrush}" />
                        <TextBlock Text="{loc:Tr Remote.Files.DeleteWarning.Body}"
                                   TextWrapping="Wrap"
                                   FontSize="11"
                                   Foreground="{DynamicResource WarningTextBrush}" />
                    </StackPanel>
                </Border>

                <!-- Selection Controls -->
                <Grid Grid.Row="1" ColumnDefinitions="Auto,Auto,*" ColumnSpacing="5" Margin="0,0,0,12">
                    <Button Grid.Column="0"
                            Content="{loc:Tr Remote.Files.SelectAll}"
                            Classes="focused"
                            Command="{Binding SelectAllCommand}"/>

                    <Button Grid.Column="1"
                            Content="{loc:Tr Remote.Files.DeselectAll}"
                            Classes="danger"
                            Command="{Binding DeselectAllCommand}"/>
                </Grid>

                <!-- Files -->
                <ScrollViewer Grid.Row="2">
                    <ItemsControl ItemsSource="{Binding FilteredFiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="12"
                                        Margin="0,0,0,8"
                                        Cursor="Hand"
                                        PointerPressed="OnFileRowPressed">

                                    <Grid ColumnDefinitions="Auto,*,Auto,Auto">

                                        <!-- Индикатор выбора: клик по всей строке переключает (см. OnFileRowPressed) -->
                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsSelected}"
                                                  IsHitTestVisible="False"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,12,0" />

                                        <!-- File Info -->
                                        <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FileName}"
                                                       FontWeight="Medium"
                                                       FontSize="13" />

                                            <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}">
                                                <Run Text="{loc:Tr Remote.Files.Modified}" />
                                                <Run Text="{Binding FormattedDate}" />
                                                <Run Text=" • " />
                                                <Run Text="{Binding FormattedSize}" />
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- Owner -->
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Owner}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="12,0" />

                                        <!-- Permissions -->
                                        <TextBlock Grid.Column="3"
                                                   FontSize="11"
                                                   FontFamily="Consolas,Courier New,monospace"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center">
                                            <Run Text="{Binding Permissions.OwnerCanRead, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=r}" />
                                            <Run Text="{Binding Permissions.OwnerCanWrite, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=w}" />
                                            <Run Text="{Binding Permissions.OwnerCanExecute, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=x}" />
                                            <Run Text="{Binding Permissions.GroupCanRead, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=r}" />
                                            <Run Text="{Binding Permissions.GroupCanWrite, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=w}" />
                                            <Run Text="{Binding Permissions.GroupCanExecute, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=x}" />
                                            <Run Text="{Binding Permissions.OthersCanRead, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=r}" />
                                            <Run Text="{Binding Permissions.OthersCanWrite, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=w}" />
                                            <Run Text="{Binding Permissions.OthersCanExecute, Converter={StaticResource BoolToPermissionConverter}, ConverterParameter=x}" />
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3"
                Background="{DynamicResource SurfaceBrush}"
                Padding="20"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0">

            <Grid RowDefinitions="Auto,Auto" RowSpacing="12">

                <!-- Сводка по выбору: статус слева, метрики справа (заполняет нижнюю часть) -->
                <Border Grid.Row="0"
                        Background="{DynamicResource AppBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="14,10">
                    <Grid ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="24">

                        <TextBlock Grid.Column="0"
                                   Text="{Binding StatusMessage}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextSecondaryBrush}" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="1">
                            <TextBlock Text="{loc:Tr Remote.Files.Selected}"
                                       FontSize="10"
                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                            <TextBlock FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}">
                                <Run Text="{Binding SelectedCount}" />
                                <Run Text="{loc:Tr Remote.Files.FilesUnit}" />
                            </TextBlock>
                        </StackPanel>

                        <Border Grid.Column="2"
                                Width="1"
                                Background="{DynamicResource BorderBrush}"
                                Margin="0,2" />

                        <StackPanel Grid.Column="3" VerticalAlignment="Center" Spacing="1">
                            <TextBlock Text="{loc:Tr Remote.Files.TotalSize}"
                                       FontSize="10"
                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                            <TextBlock FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       Text="{Binding SelectedTotalSize, Converter={StaticResource BytesToReadableSizeConverter}}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Buttons -->
                <Grid Grid.Row="1" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="12">

                    <Button Grid.Column="1"
                            Content="{loc:Tr Remote.Files.DownloadAndProcess}"
                            Classes="focused"
                            MinWidth="190"
                            Command="{Binding ConfirmCommand}"/>

                    <Button Grid.Column="2"
                            Content="{loc:Tr Remote.Files.Cancel}"
                            Classes="danger"
                            Command="{Binding CancelCommand}"/>
                </Grid>
            </Grid>
        </Border>

    </Grid>
</UserControl>
